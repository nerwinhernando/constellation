<h1><%= @plan.title %></h1>

<%= render "progress", plan: @plan %>

<p>
  <%= link_to "New Phase",
      new_workspace_plan_phase_path(
        @workspace,
        @plan
      ) %>
</p>

<%= render @phases %>
