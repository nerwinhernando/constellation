<h1><%= @plan.title %></h1>

<%= turbo_frame_tag dom_id(@plan, :progress) do %>
  <%= render "progress", plan: @plan %>
<% end %>

<p>
  <%= link_to "New Phase",
      new_workspace_plan_phase_path(
        @workspace,
        @plan
      ) %>
</p>

<%= render @phases %>
